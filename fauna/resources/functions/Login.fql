Update(Function("Login"), {
  role: "admin",
  body: Query(
    Lambda(
      ["email", "password"],
      Select(
        ['secret'],
        Login(
          Match(
            Index("unique_User_email"),
            Var("email")
          ),
          {
            password: Var("password")
          }
        )
      )
    )
  )
});